{
  "name": "pixi-magic-extension",
  "version": "0.1.0",
  "description": "JupyterLab extension that detects %pixi_install and changes the cell type to Raw",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "jupyterlab": {
    "extension": true
  },
  "dependencies": {
    "@jupyterlab/application": "^4.0.0",
    "@jupyterlab/notebook": "^4.0.0",
    "@lumino/widgets": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
